<!DOCTYPE html>
<html>
<head>
  <script src="http://code.jquery.com/jquery-1.11.1.min.js"></script>
  <script src = "login.js" type="text/javascript"></script>
</head>
<style>
span{
  font-size: 15px;
  color: #ffff;
  background-color: #2881DB;
  padding: 10px;
}
.w3-input {
    width: 130px;
    height: 30px;
    display: block;
}
#email-error{
  color: red;
}
#phone-error{
  color: red;
}
</style>
<script>
</script>
<body>
<span>Hello <%= loginData.name %> ! Complete your profile
  </span>
  <input type="hidden" name="name" value=<%= loginData.name %> id ="name">
  <input type="hidden" name="pass" value=<%= loginData.pass %> id = "pass">
  
  <p>
    <label class="w3-text-blue"><b>Email</b></label>
    <input class="w3-input w3-border" name="email" type="email" id ="email" placeholder="exmple@gmail.com" autofocus/></p>
    <div id='email-error'></div>
    <p>
    <label class="w3-text-blue" id = "pass"><b>Mobile Number</b></label>
    <input class="w3-input w3-border" name="phone" type="Number" id ="phone" placeholder="phone" autofocus /></p>
    <div id='phone-error'></div>
    <p>
      <p>
      <label class="w3-text-blue" id = "pass"><b>Education</b></label>
      <input class="w3-input w3-border" name="education" type="text" id ="education" placeholder="education" autofocus /></p>
      <div id='education-error'></div>
      <p>
      <label class="w3-text-blue" id = "pass"><b>Area of Intrest</b></label>
      <input class="w3-input w3-border" name="aoi" type="text" id ="aoi" placeholder="Area" autofocus /></p>
      <div id='area-error'></div>
      <p>
    <button class="w3-btn w3-blue" id="saveBtn" onclick="valitation()" type="Save">Save</button></p>

    <script type="text/javascript">
      var valitation = function(){
        var email = document.getElementById('email').value;
        
        var phone = document.getElementById('phone').value;
        var education=document.getElementById('education').value;
        var aoi=document.getElementById('aoi').value;
        
          
            
            var emailFilter = /^[^@]+@[^@.]+\.[^@]*\w\w$/;
        // var illegalChars = /[\(\)\<\>\,\;\:\\\"\[\]]/;
          if(email=='')
          {
            // email.style.background-color='yellow';
            document.getElementById('email-error').innerHTML='enter email';
          }
          else if (!emailFilter.test(email)) {
              // email.style.background = 'Yellow';
              document.getElementById('email-error').innerHTML = "Please enter a valid email address.";
           
           // else if (!emailillChars.math(email)) {
           //      // email.style.background = 'Yellow';
                 
           //       document.getElementById('email-error').innerHTML = "Email contains invalid characters.";
          }else
          {
            // email.style.background-color='white';
            document.getElementById('email-error').innerHTML='';
          }

          
          if(phone=='')
          {
            // phone.style.background='yellow';
            document.getElementById('phone-error').innerHTML='enter phone Number';
          }else if (phone.length<10)
          {
            document.getElementById('phone-error').innerHTML='the Number is short';
          }else
          {
            // phone.style.background='white';
            document.getElementById('phone-error').innerHTML='';
          }
          if(education='')
          {
            // education.style.background='yellow';
            document.getElementById('education-error').innerHTML='enter education';
          }else
          {
            // education.style.background='white';
            document.getElementById('education-error').innerHTML='';
          }
          if(aoi='')
          {
            // aoi.style.background='yellow';
            document.getElementById('area-error').innerHTML='enter area';
          }else{
            // aoi.style.background='white';
            document.getElementById('area-error').innerHTML='';
      }
    }
      
    
        
  </script>
</body>
</html>